import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  linkText: {
    color: '#ffd700',
    textDecorationLine: 'underline',
    fontSize: 16,
    marginTop: 5,
  }
});
