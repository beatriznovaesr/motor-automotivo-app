import { StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  title : {
    fontFamily:'RobotoSerif_700Bold',
    fontSize: 25,
    color: "#FFFFFF",
    paddingTop:0,
    marginBottom: 10,
    marginTop: 0,
    textShadowColor: '#26434f',
    textShadowOffset: { width: 0, height: 1 },
    textShadowRadius: 10,
  },

  view: {
    height:40,
    marginTop: -70,
    marginBottom: 20
  }
});
